# Check Before You Run

The environment check verifies:

- workspace trust
- the configured Kotlin command and version
- the selected execution mode
- Gradle project and command availability when relevant

If a check fails, use the notification actions to open settings, setup help, or the Kotlin Worksheet log.
