# Run from the Editor

Open a `.worksheet.kts` file and select the Run button in the editor title.

Progress shows environment detection, Gradle classpath resolution when needed, Kotlin execution, and result application. The status bar keeps the final outcome and duration.
