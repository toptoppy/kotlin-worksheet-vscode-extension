# Start with a Worksheet

Kotlin Worksheet files end with `.worksheet.kts`.

The New Worksheet command creates a small example that demonstrates values, expressions, and printed output. In a multi-root workspace, choose the folder where the file belongs.
