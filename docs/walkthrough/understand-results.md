# Read the Results

Inline mode writes generated comments next to evaluated code:

```kotlin
val answer = 40 + 2 // => 42
```

Decoration mode shows the same result without changing the file. Use `Kotlin Worksheet: Toggle Render Mode` to switch modes, then run the worksheet again.
